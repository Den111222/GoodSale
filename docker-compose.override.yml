services:
  elasticsearch:
    environment:
      - discovery.type=single-node
#      - ES_JAVA_OPTS=-Xms1g -Xmx1g
      - xpack.security.enabled=false
    ports:
      - '9200:9200'

  postgres:
    ports:
      - "5432:5432"

networks:
  default:
    driver: bridge